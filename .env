# In all environments, the following files are loaded if they exist,
# the latter taking precedence over the former:
#
#  * .env                contains default values for the environment variables needed by the app
#  * .env.local          uncommitted file with local overrides
#  * .env.$APP_ENV       committed environment-specific defaults
#  * .env.$APP_ENV.local uncommitted environment-specific overrides
#
# Real environment variables win over .env files.
#
# DO NOT DEFINE PRODUCTION SECRETS IN THIS FILE NOR IN ANY OTHER COMMITTED FILES.
# https://symfony.com/doc/current/configuration/secrets.html
#
# Run "composer dump-env prod" to compile .env files for production use (requires symfony/flex >=1.2).
# https://symfony.com/doc/current/best_practices.html#use-environment-variables-for-infrastructure-configuration

###> symfony/framework-bundle ###
APP_ENV=dev
APP_SECRET=
###< symfony/framework-bundle ###

SHOPWARE_HOST=https://myshopwareshop.com
SHOPWARE_CLIENT_ID=client_id
SHOPWARE_CLIENT_SECRET=client_secret

###> symfony/messenger ###
MESSENGER_TRANSPORT_DSN=doctrine://default?auto_setup=1
###< symfony/messenger ###

DATABASE_URL="sqlite:///%kernel.project_dir%/data/queue_%kernel.environment%.db"

# Printer connection DSN:
#   file:///dev/usb/lp0      local USB/serial device (or any writable file path)
#   tcp://192.168.1.50:9100  network printer or Pi gateway speaking raw ESC/POS (port defaults to 9100)
#   dummy://                 no printer, receipts are only archived in DATA_DIR
PRINTER_DSN=file:///dev/null
# Optional: name printed on the printer:test receipt (defaults to the SHOPWARE_HOST domain)
#SHOP_NAME="Masala Mio"

CODECOV_TOKEN=Token

DATA_DIR="/data/receipts/"
# Receipt copies contain personal data: delete them after this many days (0 = keep forever)
RECEIPT_RETENTION_DAYS=30
# Uptime Kuma push URL for the heartbeat (every minute, see docs/monitoring.md); empty = off
HEARTBEAT_URL=
# Minimum level written to stderr as JSON lines: debug | info | notice | warning | error
LOG_LEVEL=notice
